{erl_opts, [debug_info]}.
{deps, [
    {cargo,
        {git, "https://github.com/rusterlium/erlang-cargo.git", {branch, master}}
    }
]}.

{eunit_tests, [rebar3_cargo]}.

{ct_opts, [
    {dir, "ct"}
]}.

{dialyzer, [
    {warnings, [unknown]}
]}.
